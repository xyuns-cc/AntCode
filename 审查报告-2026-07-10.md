# AntCode v1.0.0 深度审查报告

日期：2026-07-10 · 方式：7 个并行审查 agent 按子系统全维度走查（逻辑/复杂度/性能/可维护性/安全/功能），主报告作者抽查核验了最严重的 P0/P1 项。

范围：packages/antcode_core (38K) · antcode_scrapy (1.8K) · contracts · web_api (12.8K) · master (5K) · gateway (3.5K) · worker (27K) · frontend (35K)。

> 说明：**「态」** 列标注该缺陷当前是否会触发 —— `live`=正常路径可触发；`latent`=真实 bug 但当前无调用方/需特定部署形态；`hardening`=纵深加固项。severity 已由主报告作者按「利用前提 + 实际影响」重新校准，可能与子 agent 原评级不同。

---

## 摘要

- 累计发现 **60+** 项，其中 **P0 级 7 · P1 级 20 · P2 级 ~20 · P3 级 ~15**。
- **底盘是扎实的**：之前 R1–R8 / T6 / T7 修过的安全项（登录限流+锁定、密钥轮换、子进程 env 双层脱敏、git URL 白名单、gateway 明文端口 fail-closed、Redis per-worker ACL、zip-slip 防护、XFF 受信代理白名单）经独立核对**真实且无绕过**。
- **最需要立刻处理的 5 项**（live + 可利用/会损坏数据）：
  1. `report_task_status` / `dispatch_batch` / `disconnect_worker` 三个 worker 端点授权缺口（越权 + 集群 DoS）
  2. `duplicate_project` 引用已删字段 → 复制 FILE 项目必 500
  3. render 插件模板路径 f-string 注入 → 打破 render-only worker 的能力边界
  4. scrapy gateway 模式内容去重**静默失效** + 并发去重击穿
  5. master `reconcile` 裸 bulk update 绕过终态 CAS → 终态复活/翻转
- **系统性技术债**（贯穿多子系统）：① 大量「双实现 / 死代码」（两套 StreamClient、两套 serialization、两套 HMAC、两套 WebSocket、RetryService、websocket.ts）；② 「进程内状态」在多副本/多 worker 部署下静默失效（worker_auth、gateway `_event_id_map`、告警配置、rate-limit 计数）；③ 「内存分页」反模式（`.all()` 全量拉取后 Python 切片）散布在 web_api/core。

---

## P0 — 立即修（live 且高危，或随时会爆的哑弹）

| # | 位置 | 缺陷 | 态 |
|---|---|---|---|
| P0-1 | web_api/routes/v1/workers.py:1810 `report_task_status` | worker 上报只验「是合法 worker」，`_ = auth_context` 丢弃，不校验 `run_id` 是否派给该 worker → 拿任一 worker 凭证可篡改任意任务终态/日志（横向越权） | live |
| P0-2 | web_api/routes/v1/workers.py:1439 `dispatch_batch_to_worker` | 批量派发 `request: dict` 无 owner/admin 校验（对照单条 `dispatch_task_to_worker` 有 owner 校验）→ 任意登录用户在任意 worker 上跑他人项目 | live |
| P0-3 | worker/plugins/render/plugin.py:193,216 | `template_path`/`output_file`/`template_dir` 原样 f-string 拼进生成的 Python 源码（仅 context 用 repr）→ 注入任意代码，打破 render 沙箱；配合 T6 能力路由，「render-only worker」信任边界被击穿 | live |
| P0-4 | worker/runtime/uv_manager.py:131,334 | `env_name` 来自 payload 未白名单校验即 `os.path.join`+`shutil.rmtree` → `delete_env("../../../.ssh")` 删 venvs_dir 外任意目录 | live |
| P0-5 | scrapy/pipelines/dedup_pipeline.py:120 + redis_pipeline.py:175 | gateway sink 无 `_redis` 属性，去重「第二阶段 SADD」永不执行 → **gateway 模式跨 run 内容去重完全静默失效**；且两阶段提交在 CONCURRENT_REQUESTS>1 下 check-then-act 竞态击穿 | live |
| P0-6 | master/control/reconcile_loop.py:218-251 | `_check_inconsistent_states`/`_cleanup_zombie_tasks` 用裸 `.update()` 绕过 `execution_status_service` 终态保护 CAS → 迟到的 worker 结果与 reconcile 互相覆盖，终态在两路径间翻转/复活 | live |
| P0-7 | core/infra/redis/stream_client.py JsonCodec | 序列化把 `bool` 当 `int` 透传（`isinstance(True,int)==True`），Redis 存成 `"True"`，消费端 `if data["success"]` 对 `"False"` 恒为真 → 失败任务被判成功 | live（需确认哪些 payload 带 bool 过流） |

> 另有两个「latent P0 哑弹」：`queue_service.py` 全文导错状态枚举（`TaskStatus.DISPATCHED` 不存在，任一调用即 AttributeError，当前队列无消费者）；`test_service.py` 的 `TaskRun.raw(sql, batch_id)` 与 Tortoise API 不符（第二位参是 DB 连接不是查询参数，测试执行链路会恒失败）。前者是死路径，后者若「测试执行」功能已接线则升为 live P0 —— **建议优先核实 test_service 是否在用**。

---

## P1 — 尽快修（live 功能损坏 / 真实并发错误 / 多副本失效）

### 授权与越权
- **[web_api] workers.py:559 `disconnect_worker`** 仅需登录无 admin（相邻 update/delete 都有）→ 普通用户遍历 worker_id 逐个断开 = 全集群 DoS。**两个 agent 独立发现，已核实**。
- **[web_api] workers.py:1512/1553** `update_worker_task_priority` / `cancel_worker_queued_task` 队列写操作仅需登录无 admin。
- **[web_api] crawl.py:906 `get_system_metrics`** 唯一漏掉 `_verify_project_access` 的带 project_id 端点 → IDOR 读他人爬取指标。

### 功能损坏（live）
- **[web_api] project.py:989 `duplicate_project`** 事务内 `ProjectFile.create(file_path=…, original_name=…, file_size=…)` 引用**已被迁移删除**的字段 → 复制 FILE 项目必 500，功能 100% 失效。
- **[web_api] websocket_logs.py:154 `/ws/cleanup`** 调用不存在的 `cleanup_inactive_connections`（实际是私有 `_cleanup_inactive_connections`）→ AttributeError 死端点。
- **[worker] runtime/uv_manager.py:29 `PACKAGE_PATTERN`** 正则损坏（raw string 里字面反斜杠 + 错误 range），连 `"requests"` 都不匹配，fail-closed → **运行时包管理整体不可用**。
- **[core] infra/redis/factory.py:238 sentinel** `master_for()` 未传 password/db → 哨兵+密码部署 `NOAUTH`，文档声称支持的 sentinel 模式实际不可用。

### 并发 / 分布式正确性
- **[core] resilience/circuit_breaker.py:290 `__aenter__`** HALF_OPEN 递增 `_half_open_calls` 但 `__aexit__` 不递减 → 断路器可能长期卡半开拒绝合法请求（`redis_breaker_guard` 正走此路径）。
- **[core] scheduler/execution_status_service.py:214** success/failure 计数非原子读改写 + 全字段 save → 并发终态回报丢计数/互相覆盖。应 `F("success_count")+1`。
- **[core] crawl/batch_service.py:690** 取消批次清的是**项目级**去重集/队列 → 污染同项目其它运行中批次（B 的去重指纹被清，全部重抓）。
- **[master] control/lease_sweeper_loop.py:100** 无 leader gate，每个 master 实例都 sweep → 多副本下重复 `_on_worker_evicted`，抖动时误杀正常任务 ×N。
- **[master] control/retry_loop.py:260** naive `datetime.now()` 比较 aware `retry_time` → `TypeError` 被吞，重试永不触发。
- **[gateway] control_service.py:410 WatchControl** 无 PEL 排空 → worker 重连前未 ACK 的 cancel/kill 事件永久丢失，控制指令静默失效。
- **[gateway] control_service.py:91 `_event_id_map`** 控制事件 ACK 依赖进程内 map → 多 gateway 副本 / 重启后 AckControl 恒失败 → 事件无限重投。与「gateway 数据面多副本」目标直接冲突。

### worker 判活 / split-brain
- **[worker] heartbeat/reporter.py:358** lease 被服务端 `revoked` 时只当普通心跳失败，engine 继续 poll+执行 → master fence 后老 worker 仍跑 = **split-brain 同任务双跑**。
- **[worker] heartbeat/reporter.py:315** 心跳固定 30s 且忽略 `renew_after_ms`，而 lease TTL=30s（renew_after 建议 10s）→ 续租周期==TTL，一次 DEGRADED 抖动强制 60s 间隔即超期被回收。
- **[worker] heartbeat/reporter.py:297** `set_disconnect/reconnect_callback` 是死代码无人调用 → transport DEGRADED 时 engine 不停拉新任务 → 接下无法上报的孤儿 run。
- **[worker] projects/fetcher.py:126 + engine.py:790** workspace 按 run_id 且每次 fetch 先无锁 `rmtree` → at-least-once 同 run_id 并发时后者删掉前者运行中的目录。

### 无界内存（live 泄漏）
- **[web_api] websocket_connection_manager.py:70** `_locks` / MessageQueue `_queues`/`_dropped_count` 按 run_id `defaultdict` 累积，从不回收 → 高基数 run_id 长跑泄漏。
- **[worker] logs/manager.py:130** 每条日志行 `asyncio.create_task(_dispatch_entry)`，任务集合无界 → 高速日志下 pending task 爆炸 OOM。
- **[worker] engine/engine.py:164 (limits.py:161)** `ConcurrencyLimiter.release()` fire-and-forget `create_task` 不保引用 → 计数漂移 / loop 关闭窗口抛错。

### 安全（core / scrapy / frontend）
- **[core] security/secret_box.py:47** Fernet 密钥用 `base64(sha256(key))` 单轮无盐无 KDF 派生 → ENCRYPTION_KEY 若低熵，DB 密文可离线爆破。**（已核实；影响取决于 key 熵，属弱密钥加固）**
- **[core] security/login_crypto.py:129** 登录私钥读取失败即**静默重生并覆盖磁盘** → 多副本/瞬时 FS 错误下全站登录解密失败且旧密钥永久丢失。**（已核实）**
- **[core] security/login_guard.py:88** 失败计数窗口固定 900s 不滚动 → 每 899s 试 4 次的慢速爆破完全绕过账户锁定。
- **[scrapy] sinks/gateway_sink.py:123** 无后台定时 flush，慢速/收尾爬虫的 buffer 数据滞留甚至随进程退出丢失；且 batch 失败按「1 次失败」计，阈值与 written 统计在批模式下失真。
- **[frontend] Login/index.tsx:119 + utils/crypto.ts** 「记住密码」用可逆 XOR（密钥源自公开设备指纹）把**明文密码**存 localStorage → XSS/恶意扩展可离线还原。**（已核实）**
- **[frontend] ProjectCreateDrawer.tsx:150 ↔ RuleProjectForm.tsx:275,346** 未 memo 回调 + 依赖回调的 effect → 建项目 Drawer 打开即持续渲染循环，输入卡顿。
- **[frontend] Tasks/ExecutionLogs.tsx:181** 自动刷新终态判断漏 `timeout`/`cancelled` → 永久 10s 轮询打后端。

---

## P2 — 计划内修（性能 / 多副本 / 健壮性 / 复杂度）

**性能（内存分页反模式）**
- core/crawl/batch_dispatcher_service.py:268 `_already_dispatched_urls` 全表扫 `TaskRun.all()` 到内存（随历史线性劣化）。
- web_api/tasks.py:548 `get_task_dependencies`、:1195 `get_task_execution_logs`（全量读日志再 splitlines 切片）、workers.py:1683 render worker 分页 —— 均 `.all()` 后 Python 切片，应下推 DB。
- core/crawl/metrics_service.py:583 `get_metrics_summary` 重复采集系统指标两次。

**多副本 / 进程内状态失效**
- core/security/worker_auth.py:29 secret/rate_limit/nonce 全进程内 dict → 多 worker 进程注册在 A 请求打到 B 即拒；限流 = 配置值×进程数；`_run_async` 用 `asyncio.run` 关掉共享 redis 单例的 loop（nonce 防重放退化到进程内 dict，重启即失忆）。
- web_api/crawl.py:1162 `update_alert_config` 存进程内 service 状态 → `SERVER_WORKERS>1` 时各进程阈值不一致；且用 Query 传 body 语义数据。
- gateway/rate_limit.py:276 对所有 RPC 一律返回 unary handler → server/client-stream 限流命中时 codec 类型不匹配（auth.py 已按 4 类分派修好，rate_limit 漏修）。

**安全加固**
- gateway/rate_limit.py:267 限流键信任 gRPC metadata 的 XFF 无受信代理校验（web_api 侧有白名单，gateway 没有）→ 免鉴权的 Register 可伪造 XFF 绕过限流洪泛。
- core/security/api_key.py:73 worker `api_key` 明文存库（`hash_api_key` 已存在却没用）→ DB 只读泄漏即全 worker 凭证泄漏。
- worker/builder.py:174 requirements/constraints 零校验即 `uv pip install -r` → `--index-url http://attacker` 供应链投毒 / `-r /etc/shadow`。
- worker/plugins/spider/plugin.py:115 用户 `settings` dict 整体展开成 `-s KEY=VALUE` 无白名单 → 可关 robots/改代理放大 SSRF。
- worker/plugins/render|spider entry_point 缺路径穿越校验（code 插件的 `_validate_entry_point` 未复用）。
- worker/security/secrets.py:184 先 `write_text`(0644) 再 chmod(0600) 有 world-readable 窗口；identity.yaml 无 chmod。
- worker/runtime/manager.py:87 RuntimeLock/GC in-use 判定是进程内 `asyncio.Lock`（跨进程 FileLock 已实现却没接线）→ 多 worker 共享 venvs_dir 时 GC 误删他进程在用的 venv。
- scrapy/rule_spider.py:356 ReDoS 启发式只拦一种模式，漏 `(a+)+`/`(.*)*` 等且 stdlib re 无超时。

**健壮性 / 数据**
- worker/transport/redis/reclaim.py:213 dead-letter 判据 `delivery_count > max_retries`，但每次 XAUTOCLAIM 都自增 → 合法长任务跨重启被认领 4 次即误进死信丢失。
- worker/plugins/spider/data/reporter.py:180 整批一次性 extend buffer，Redis 写失败静默丢数据但 run 显示成功。
- worker/projects/fetcher.py:153 解压只校验压缩包自身，不检查解压后总量（zip/tar bomb，RLIMIT_FSIZE 只限单文件）。
- master/gateway/handlers/poll.py:207 dispatch PEL 重连排空无最大投递上限 → 「收到即崩溃」的孤儿无限重投永不进死信。
- scrapy/sinks/redis_sink.py:110 stream TTL 只在首写刷一次 → 长跑 run 超 24h 时旧数据整体过期而新数据仍在写（数据断档）。
- frontend/stores/authStore.ts:102 persist 把 `isAuthenticated`/`permissions` 存 localStorage → token 失效后仍从本地恢复放行管理 UI（可手改解锁前端入口，后端仍拦）。

**复杂度热点（建议拆分）**
- master/control/scheduler_loop.py **1489 行**：CRUD 查询 + APScheduler + 单次执行状态机 + 重试排程四合一，且重试逻辑与 retry_loop 双实现。
- web_api/routes/v1/workers.py **2105 行**：P0 越权聚集地，admin 校验声明式与手写 `is_admin` 混用（漂移出 3 处漏加）。
- frontend/pages/Monitor/index.tsx **1773 行** 且顶部 `// @ts-nocheck` 关类型检查；RuleProjectForm 1339、Workers 1170。
- frontend/VirtualLogViewer.tsx 742 行手写伪虚拟化（全量 ResizeObserver + O(n) 位置重算），建议换 `@tanstack/react-virtual`。

---

## P3 — 清理（死代码 / 一致性 / 小瑕疵）

- **双实现/死代码**：`common/serialization.py` 与 `common/utils/serialization.py` 逐字节相同；`common/security.py` 与 `security/hmac_utils.py` 重复 HMAC；`infra/redis/streams.py` 与 `stream_client.py` 两套 StreamClient；frontend `services/websocket.ts`（382 行）全死（实时日志走 `logs.ts`）；core `RetryService` 整个无调用方（与 redispatch 重叠）且 `send_alert` 参数名不符；master 三个 loop `from ...leader import ensure_leader` 死导入；scheduler_event `_deliveries`、retry_loop 补偿框架死代码；frontend AgentProjectForm/SpiderStatsCard 无引用组件。
- **配置默认值偏宽松**：`config.py:98 BIND_HOST=0.0.0.0`（默认监听所有网卡）；`DEFAULT_ADMIN_PASSWORD` 默认空且不校验非空。
- **纵深加固**：`code_source._normalize_git_url` 仍允许 `file://`（与 `_validate_git_url` 不一致，当前无直接调用方 clone）；告警 webhook/SMTP/代理 URL 无私网(RFC1918/169.254.169.254)校验（需高权限，风险可控）；`get_worker_credentials` 把 api_key 明文嵌进 config_example 响应文本。
- **小瑕疵**：scheduler_service `if False:` 死分支、ONCE trigger naive datetime；`_env_int` 在 scrapy 三处重复定义；多处 `str = None` 该为 `str | None`；client.py `connect()` 里死 pool_kwargs；election `_on_leader_takeover` 是 TODO 空壳（切主后 PEL 超时任务不恢复）。

---

## 系统性主题（比单点更值得投入）

1. **分布式多副本正确性尚未真正达成** —— master 的 `reconcile` 裸 update 绕 CAS（P0-6）、lease_sweeper 漏 leader gate、fencing token 只记日志不参与写入判定；gateway 的 WatchControl PEL 丢失 + `_event_id_map` 进程内。**单实例能跑，>1 副本会重复回收/重复派发/终态翻转/控制丢失**。若近期不上多副本，可暂缓但要在文档明确「当前仅支持单 master / 单 gateway」。

2. **「进程内状态」在 `SERVER_WORKERS>1` 就破** —— worker_auth 的 secret/nonce/限流、告警配置、gateway 限流计数。刚把 web_api 默认 workers 提到 2，这些立刻踩中。凡是「限流 / 去重 / 配置 / 计数」都应落 Redis。

3. **双实现与死代码是最大维护负债** —— 至少 6 对重复实现，「改一处忘另一处」是未来 bug 的温床，且序列化的两套里 bool 问题都在。发版前值得做一次死代码清扫。

4. **内存分页反模式** —— 多处 `.all()` 全量拉取后 Python 切片，中低数据量无碍，是随规模劣化的定时炸弹。

---

## 建议修复顺序

**第一批（发版前必修，live 可利用/会损坏数据）**
1. P0-1/2 + P1 disconnect_worker：三个 worker 端点补授权（run_id 归属 + owner/admin）—— 都在 workers.py，一次收口。
2. P1 duplicate_project 引用已删字段（复制 FILE 项目必崩）+ `/ws/cleanup` 方法名。
3. P0-6 reconcile 走终态 CAS；P1 lease_sweeper 补 leader gate；P1 retry_loop datetime UTC。
4. P0-3 render 路径 repr；P0-4 env_name 白名单；P1 PACKAGE_PATTERN 正则修复。
5. P0-5 scrapy 去重改 SADD 原子判存 + gateway 模式补 commit 或显式禁用告警。

**第二批（硬化，尤其上多副本前）**
6. worker_auth / 告警配置 / gateway 限流从进程内迁 Redis。
7. worker 判活三件套（revoked 停机 / 续租周期读 renew_after_ms / disconnect 回调接线）。
8. P0-7 bool 序列化；circuit breaker 半开计数；execution 计数原子自增。
9. 核实 test_service.raw() 是否 live，是则修。

**第三批（债务清扫）**
10. 删 6 对双实现/死代码；拆 scheduler_loop / workers.py / Monitor 巨型文件；内存分页下推 DB；BIND_HOST 默认收紧。

---

*本报告为工作产物，未提交仓库。逐条修复可按需拆成独立任务。*
